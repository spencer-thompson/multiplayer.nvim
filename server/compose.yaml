services:
  # redis:
  #   image: redis:latest
  #   ports:
  #     - "6379:6379"

  # reflector:
  #   build:
  #     dockerfile: Dockerfile
  #   command: websocketd --port=8080 lua server.lua
  #   command: nvim
  #   ports:
  #     - "8080:8080"

  nvim:
    build:
      context: ../
      dockerfile: ./server/Dockerfile
    command: nvim -u ./init.lua --headless --listen 0.0.0.0:6666
    ports:
      - "6666:6666"
